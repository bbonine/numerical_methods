#########################################
# Homework 3 README			#
# ASTR 5900: Numerical Methods		#
# Date: 3/04/2021			#
# Author: Brett Bonine			#
#########################################

--------------------------------------------------------------------------------

TARBALL CONTENTS:

--------------------------------------------------------------------------------

-homework_3.pdf: Type set explanation of my solution to the problems


-src: Folder housing the source code for any executables in the assignment.

	-makefile
	
	*.h,*.cpp


-outputs: Folder containing my versions of the textfiles generated by my
	   algorithms.


-plots.ipynb: Jupyter notebook used to read-in and plot outputs of the 
	     executables
--------------------------------------------------------------------------------

INSTRUCTIONS:

--------------------------------------------------------------------------------

To compile the code, simply input "make" into the terminal while in the source ddirectory. (Due to budget cuts, you need to put yourself in the src folder; the makefile is stashed there).

The makefile will generate two executables: 'euler' and 'intbsstep'. The latter will prompt you for inputs, but I've provided the values I used in the writeup in parentheses if you would to use those.

The two executables will generate output text files. I've included the ones I generated myself in the 'output' directory, but you are more than welcome to make your own.

The plot heavy nature of this assignment means that the Python code comes in in the form of a Jupyter notebook this time around. I am unsure how to run these onschooner at this time, but I imagine it's something similar to loading the anaconda module with "module load /python/anaconda3"

------------------------------------------------------------------------------

MODULE INFO 
------------------------------------------------------------------------------

The following modules are loaded in my .zshrc profile:

module load python
module load impi/2019.7.217-iccifort-2020.1.217
module load imkl/2020.1.217-iimpi-2020a
module unload numactl/2.0.13-GCCcore-9.3.0   UCX/1.8.0-GCCcore-9.3.0
module load CMake
